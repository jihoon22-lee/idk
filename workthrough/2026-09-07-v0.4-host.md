# B03 — 실제 터미널 화면·사용자 호스트·재접속

WP03/#42, R01~07/14/16, S01/S02/S05/S06의 호스트와 화면 연결을 구현한다. 사용자 호스트가
PTY를 유지하고 TUI는 현재 셀·커서·모드와 generation을 받아 그린다. UI 종료는 detach이고 기존
셸에 다시 접속할 때 source/cd를 반복하지 않는다. 임시 정의, 제거한 정의의 살아 있는 셸도 목록에서
접속하며 종료·불명확 상태의 재실행은 명시적 확인을 거친다.

입력 소유 epoch, 실행 파일 SHA와 host UUID, bounded frame/queue/절대 deadline, 변경 요청의
한시적 중복 결과 보관을 연결했다. 입력 실패 뒤 자동 재전송하지 않으며 빠른 타이핑은 동일 대상/
epoch별로 묶는다. 초기화 준비는 별도 worker에서 수행하고 actor가 기록한 뒤 bootstrap을 전달한다.
기본 터미널은 순서대로 준비하고 입력 대기/미확인은 일시 중지한다. 종료 검토는 정확한 대상 목록과
실제 exit를 구분한다. ledger는 환경·입력·PID를 보관하지 않고 host 교체 시 이전 live를 Unknown으로
복원한다. 로그 저장이나 프로세스 메모리 복원이 아니다.

검토·실행에서 발견한 결함을 함께 수정했다. IPC 성공 `null` payload가 사라지던 역직렬화,
가득 찬 socket backlog와 FIFO identity 검사 대기, 같은 큰 바이너리의 중복 hash로 인한 debug
startup timeout, 빠른 키 입력 누락, 임시 종료 기록의 용량 오계산과 saved/transient ID 충돌을 다뤘다.
출력 제한/실제 I/O 오류와 종료 화면의 보관 만료를 표시한다. 최종 PTY 수집을 EOF 전에 끝내면
잘림 가능성을 표시하고 generation을 바꿔 이미 연결한 화면에도 전달한다.

## 실행 증거

- WSL Ubuntu 26.04.1 / tcsh 6.24.13 / Rust 1.97.1에서 최종 workspace **97 PASS**,
  별도 실제 PTY **4 PASS**, fmt 및 모든 target Clippy PASS.
- Host 통합 **11 PASS**: 프로젝트 2개/셸 6개, source-once, 중복 입력 요청, takeover와 오래된
  입력/resize/detach 거부, 초기화 대기와 실패 격리, host 장애/Unknown, 임시 셸 다시 열기.
- 일반 background 및 중첩 HUP/TERM 무시 작업은 명시 force 뒤 실제 종료·회수를 확인했다.
  다른 프로젝트·외부 프로세스·별도 setsid 작업은 보존된다. WNOWAIT leader anchor 회귀 PASS.
  새 SID로 분리된 자식은 정리 대상이 아니며 해당 종료 상태도 SID monitor가 회수하지 않는다.
- UI 기존 12개와 신규 실제 터미널 6개 PASS. 실제 PTY의 F1/application cursor/메뉴 키 전달,
  한글 alternate screen/resize, Ctrl-Z/fg/Ctrl-C, 화면 재접속 뒤 동일 PID/변수/source 횟수 포함.
- CLI 연결→상태 변경→detach/중복 start→close/reopen→프로젝트 정의 제거 후 셸 접속→호스트
  종료 흐름 PASS. 무관한 fixture 프로세스가 살아 있음을 확인했다.
- 최종 출력 잘림/generation 회귀, 출력 제한 및 화면 만료 UI 검증 PASS.
- 정적 development 후보 3,947,208 bytes,
  SHA-256 `eace7403605e7dacec2e228ea4bdc816bde0c85bb2eef3c210746174b678c1c0`.
  빌드 입력 hash `e5d122fc2c33fe4d9f04dd41a73455b50772061608e57dcf1308dace6a44618d`.
  로컬 smoke와 UBI 8.10 glibc 2.28 / UID 10001 / network none / 읽기 전용 컨테이너 probe PASS.
- 같은 후보의 5개 셸 준비/첫 재접속/idle RSS: 로컬 **647 ms / 89 ms / 4,740 KiB**,
  UBI **646 ms / 88 ms / 4,740 KiB**. 합성 입력 기준이며 폐쇄망 현지 성능으로 일반화하지 않는다.
- probe 강제 종료 시 probe 전용 자식 host도 끝나는 실제 검사 PASS. 정상 UI host는 detach 후 유지된다.

- 실제 UID 10001 host의 정상 handshake 뒤 fixture DAC를 완화하고, 외부 UID 1000의 raw IPC가
  SO_PEERCRED 검사에서 거부됨을 확인했다. 최초 fixture의 공유 부모 권한 오류는 올바른 차단이었으며
  test 전용 소유권을 설정해 실제 peer 검사까지 실행했다.

최종 CI 결과는 아래에 연결한다. 개발 후보는 dirty source의
실행 검증용이며 exact-main 공개 후보가 아니다. 대상 RHEL·폐쇄망 정책 실기는 공개 후 사용자
수행으로 남긴다. Git 작업 TTY/실제 source-use provider와 등록 run은 B04/B05 인계다.
