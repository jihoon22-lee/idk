# B06 — 오프라인 번들·설치 generation·복구

WP07/#46의 번들을 고정된 5개 정규 USTAR 파일로 제한하고, 사용자가 별도 신뢰 경로에서 얻은
SHA-256과 압축 bytes·모든 구성요소·manifest·라이선스 원문 inventory의 일치를 확인한다.
검증한 bytes를 메모리에 고정하므로 검토 뒤 입력 경로가 바뀌어도 새 파일을 실행하지 않는다.
checksum을 publisher 서명으로 설명하지 않는다.

설치는 새 private generation에 검증된 원본 archive도 보관한다. 단계별 journal과 원자적 current
symlink 전환, 무해한 version/schema 건강 검사로 활성화한다. 오류나 중단 복구는 설치 entrypoint만
되돌리며 살아 있는 host/run, 이후 생성된 workspace 데이터와 이전 binary를 보존한다. commit된
새 버전의 downgrade 및 schema migration은 지원하지 않고 원본을 보존한 채 거부한다. uninstall은
정확히 관리하는 두 symlink만 제거하며 generation과 사용자 데이터는 남긴다.

## 현재 실행 증거

- Rust bundle 검사 8 PASS: 압축/경로/링크/파일형식·크기 제한, digest/manifest/ELF 일치,
  input 교체 후 sealed bytes 사용, 기존 파일 보존, 라이선스 metadata 모순 거부.
- 설치 transaction 검사 6 PASS: stage/journal/activation/health/commit 각 중단 지점의 재시도,
  단계 전후 건강 검사 실패, 새 사용자 데이터 보존, 중단된 uninstall 복구, 원본 generation의
  재검증, future schema·committed downgrade·사용자 launcher 보존.
- 위 transaction 검사는 실행하지 않는 합성 ELF와 주입된 건강 검사로 상태 전이를 검증한다.
  실제 후보 실행/권한/noexec/disk-full/active host 통합 증거로 대체하지 않는다.
- 모든 target Clippy PASS. 최종 기능 통합, 실제 package 실행, CI와 공개 후보 검증은 진행 중이다.

전체 notice 수집과 실제 번들 재현 결과, 지원 환경의 설치·갱신·복구 검사 및 최종 PR/CI를 같은
작업 기록에 이어 연결한다. 대상 RHEL·폐쇄망 실기는 릴리스 후 사용자 수행이며 미실행이다.


## 실제 development 후보 검증

패키징 구현 `c2f6bb5`, 원문을 바꾸지 않는 생성 구분선 정리 `5ab857e`를 통합했다. Cargo 148개,
정적 runtime 5개, 전체 원문 296개를 수집하며 10개 Python 실패 fixture와 Python 3.10/3.14의 동일
archive 생성을 검증했다. 원문/정책/collector의 변경은 새 입력 hash와 후보를 요구한다.

B03 통합 뒤 실제 실행 후보 binary SHA-256
`49e5567039f0cd014b21fac055043f221af6fa16115f5b0768296b3bb59ba097`,
archive 1,977,237 bytes / SHA-256
`f823677d4740b4f6c46b04abed43315a6bda44a9317c8899647e4cd25e684b77`로 아래를 실행했다.

- 실제 archive를 설치한 뒤 별도 build identity fixture로 갱신: 기존 host/셸 PID와 변경한 지역 변수,
  source 1회, 원래 binary bytes 보존 PASS. 새 client의 old host 연결 거부, 원래 generation의
  재접속, entrypoint 제거 후 셸 입력 유지 PASS. 실제 PTY fixture 1개 / 3.89초.
- UBI 8.10 / UID 10001 / network none / 읽기 전용 root / Python·compiler 없음:
  실제 설치·실행·probe·제거, noexec, disk-full(1 MiB), read-only, 권한(0500) **5개 PASS**.
  성공 fixture의 실행 가능 tmpfs를 명시했고, UBI에 없는 cmp 대신 기존 sha256sum으로 원본을 확인했다.
- 갱신 fixture는 ELF 뒤에 실행되지 않는 marker만 붙여 실제 build SHA 차이를 만든 것이다.
  두 번째 공개 release 후보나 새 코드의 검증으로 주장하지 않는다.

이후 doctor의 host/설치 inventory 진단과 `--brief`를 보완했다. 해당 진단 검사와 최종 기능/CI/공개
후보 검증은 후속 결과에 연결한다. 위 hash는 통합 중간 후보이며 최종 공개 bytes가 아니다.

## 최종 기능 통합과 상태·filesystem 검증

B05 [PR #52](https://github.com/jihoon22-lee/idk/pull/52), main `fc00deb`를 통합했다.
B05의 native CI [34107586069](https://github.com/jihoon22-lee/idk/actions/runs/34107586069)와
기존 Python CI는 모든 gate PASS로 병합됐다. 위 중간 B03 후보의 hash는 현재 통합 후보가 아니다.

- `c713024`: installation schema2의 committed_version_floor는 uninstall 후에도 남고,
  미commit·실패한 높은 버전은 floor를 올리지 않는다. 실행 권한이 없거나 특수 권한을 가진
  staged binary를 거부한다. transaction8 + bundle9 PASS. 이전 schema1은 보존·거부한다.
- `568ff92`: host-sessions/runs/git-operations의 실제 typed ledger를 startup과 health가 공유한다.
  schema·UUID·중복·path/digest·단계/log bounds를 검증하며 현재 live 결과를 Unknown으로 바꾸지 않는다.
  각 파일은 독립 atomic snapshot이라 실행 중 변경과 경합할 cross-file join은 하지 않는다.
  건강 검사는 host/RunRegistry·worker·Git·초기화를 시작하지 않고 참조 로그도 건드리지 않는다.
  health3 PASS: 세 ledger와 inventory의 bytes/inode/mtime 보존, malformed/future/inconsistent
  거부, missing directory를 만들지 않음. shared decoder의 Run core17/host10/Git host4 PASS.
- `a472293`·`cc2368e`: statfs로 NFS state/runtime을 거부하고 허용된 명시적 로컬 위치를 안내한다.
  조회 실패를 정상으로 바꾸지 않고 기존 state를 옮기거나 비우지 않는다. Store6 PASS.
  unavailable locking과 단순 busy lock의 안내를 구분했다.
- `scripts/test-native-storage.py`는 실제 child에 seccomp로 ENOLCK, atomic rename EIO, statfs EIO를
  주입한다. 세 실패에서 설정/원본 bytes, pending 파일 정리, host 미시작, 정상 재조회가 검증된다.
  개발 binary에서 Python3.10/3.14 각3 PASS. 실제 static 통합 후보 결과는 아래에 연결한다.
  NFS identity는 fixture이며 실제 NFS mount/현지 정책 PASS는 아니다.
- 실제 package_install에는 살아 있는 Shell뿐 아니라 Run PID·기록·log prefix·계속되는 출력을
  업데이트와 uninstall 후 확인하고, fixture 해제 뒤 실제 성공/초기화2회를 확인하는 검사를 추가했다.
  새 최종 archive로 수행한 결과가 나오기 전에는 기존 중간 후보 PASS로 대체하지 않는다.

최종 통합 정적 후보·실제 Run 업데이트·UBI 설치/오류·native CI 결과는 아래 후속 기록을 따른다.
