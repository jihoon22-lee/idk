# v0.2.1 Hotfix Release Plan

**Goal:** Publish the already-merged TUI terminal-loss fix as the smallest coherent hotfix release, without rewriting v0.2.0 history.

**Scope:** Release metadata and current-version documentation only. No runtime behavior, dependency, workflow, or historical milestone changes belong in this PR.

## Task 1: Align version and release notes

1. Change `src/idk/__init__.py` from `0.2.0` to `0.2.1`.
2. Move the existing TUI terminal lifetime entry from `CHANGELOG.md` `[Unreleased]` into a new `## [0.2.1] - 2026-08-20` section.
3. Keep an empty `[Unreleased]` section and update comparison links:
   - `[Unreleased]` compares `v0.2.1...HEAD`.
   - `[0.2.1]` compares `v0.2.0...v0.2.1`.
4. Update the executable release example in `CHANGELOG.md` to tag `v0.2.1`.

## Task 2: Update current-version user documentation

Update only examples or checks that describe the version users should see from the current artifact:

- `README.md` quick output example.
- `docs/GUIDE.md` generated environment header and doctor brief example.
- `docs/closed-network-setup.md` installed-artifact version check.
- `docs/env-survey.md` field-acceptance commands, expected output, and status wording so tomorrow's closed-network check uses the fixed `v0.2.1` artifact.

Preserve `v0.2.0` references that document a completed milestone, feature scope, prior plan, prior changelog entry, or old release. Do not edit tests whose `0.2.0` text is merely isolated fixture data.

## Task 3: Verify and commit

Run:

```bash
rg -n --hidden --glob '!.git/**' --glob '!dist/**' 'v?0\.2\.0|v?0\.2\.1' \
  src README.md CHANGELOG.md docs .github tests
TMPDIR=/tmp TEMP=/tmp TMP=/tmp uv run --python 3.10 pytest -q
uvx ruff check .
uvx ruff format --check .
./scripts/build-pyz.sh
./scripts/smoke.sh
```

For every remaining `0.2.0` match, confirm it is historical or a fixture. Confirm `dist/idk.pyz --version` reports `idk 0.2.1`. Commit once as:

```text
chore: prepare v0.2.1 release
```

Do not push, open a PR, tag, or publish a release. Those gates remain with the root orchestrator.
